# 开发规范

1. 所有文本文件使用 UTF-8 无 BOM。
2. 不允许页面直接操作核心数据库。
3. 不允许在前端信任 `role`、`teamId`、`openId`。
4. 所有库存变化必须有流水。
5. 产品使用软删除。
6. 接口返回格式统一。
7. 每个阶段只处理自己的范围。
8. 不允许无关重构。
9. 错误不能只 `console.log`，需要转成用户可理解状态。
10. 写操作后续必须考虑重复点击和幂等。
11. 微信时间、数据库时间统一使用服务端时间。
12. UI 颜色、圆角、间距后续统一使用设计变量。
